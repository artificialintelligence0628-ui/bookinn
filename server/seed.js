// Seed data — used only the first time the server runs (empty database).
// Intentionally empty: no sample houses, apartments, or hostels (with their
// room categories like "One in a room" / "Two in a room") are pre-loaded.
// Real listings should be added through the admin app.
export const SEED_LISTINGS = [];
